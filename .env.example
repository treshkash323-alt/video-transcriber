# Скопируйте в .env и заполните ключ (файл .env не коммитится)

# openai — Whisper API (нужен ключ)
# local — faster-whisper на CPU (без оплаты, медленнее)
TRANSCRIPTION_BACKEND=openai
OPENAI_API_KEY=sk-proj-ваш_ключ_сюда

# Только для TRANSCRIPTION_BACKEND=local
WHISPER_MODEL_SIZE=base
